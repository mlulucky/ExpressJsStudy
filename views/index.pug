html
    head
        meta(charset="UTF-8")
    body
        h1 안녕! a+b
            span=a+b
        //img(src="/public/img/a.jpeg", style="width=100px")
        img(src="/img/참새.jpeg", style="width=100px")
        // 미들웨어로 정적리소스 처리시 자동으로 urlEncoding 처리가 되어 있음! 그래서 참새 라는 한국어로도 파일 소스가 불러와진다.
        
        h2 💚미들웨어로 정적리소스 처리
            ul
                li app.use(express.static(("경로"));
                li 확장자(.png,.jpeg,.hwp..)가 정적리소스인 경우 public 폴더에서 찾아서 응답
                li 응답하는 문서의 형식을 자동으로 처리한다.
        h2 💚express 에서 제공하는 템플릿 엔진 적용(pug)
            ul
                li app.set("view engine", "엔진이름");
                li express 사이트에 명시된 템플릿 엔진을 응답으로 사용
                li app.set("views", "경로") : default "./views" (템플릿 엔진 파일들의 기본 경로)
        h2 💚bodyParser 를 이용해 네트워크 요청 헤더로 넘어오는 queryString 을 미들웨어로 처리
        h3 회원가입 폼 action=/signup.do
        // get 방식 : http://localhost:7777/signup.do?id=hong123&name=%ED%99%8D%EA%B8%B8%EB%8F%99&job=%EC%9D%98%EC%A0%81&age=%3F%3F%3F // url 에 쿼리스트링을 포함
        // post 방식 : http://localhost:7777/signup.do // 요청헤더(요청에 대한 메타(상세)정보)에 쿼리스트링을 포함 // 요청헤더에있는 파라미터 id=hong123&name=%ED%99%8D%EA%B8%B8%EB%8F%99&job=%EC%9D%98%EC%A0%81&age=%3F%3F%3F
        form(action="signup.do" method="post")
            // post 방식 : 파라미터를 요청헤더에 숨긴다.
            p
                label 아이디
                input(name="id",value="hong123")

            p
                label 이름
                input(name="name",value="홍길동")

            p
                label 직업
                input(name="job",value="의적")

            p
                label 나이
                input(name="age",value="???")
            p
                button(type="submit") 제출
        h3 bodyParser 가 적용된 /signup2.do
        form(action="signup.do" method="post")
            // post 방식 : 파라미터를 요청헤더에 숨긴다.
            p
                label 아이디
                input(name="id",value="hong123")

            p
                label 이름
                input(name="name",value="홍길동")

            p
                label 직업
                input(name="job",value="의적")

            p
                label 나이
                input(name="age",value="???")
            p
                button(type="submit") 제출
        br
        br
        // 미들웨어는 public 이 이미 정해져있다
        // 파라미터는 넘어건다 -> 익스프레스 서버에서 해석해서 참새로 해석.